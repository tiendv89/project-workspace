# J15 - Switch to Feature Mode

1. The user changes the board to Feature Mode.
2. The board shows feature-level content from `GET /api/workspaces/:workspaceId/features`.
3. The user scans features by overall state and `task_counts`.

**Expected result:** the user can review feature-level progress without task-level noise.
